<?php
// module/QCM/view/qcm/question/result.phtml:

$title = $question->getTitle();
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<table class="mdl-data-table mdl-js-data-table mdl-shadow--2dp">
    <thead>
    <tr>
        <th class="mdl-data-table__cell--non-numeric">Answer</th>
        <th class="mdl-data-table__cell--non-numeric">Count</th>
    </tr>
    </thead>

    <tbody>
    <?php
    /** @var array $questions */
    foreach ($answers as $answer) :
        if ($answer->getQuestionId() == $questions->getId()) :?>
            <tr>
                <td class="mdl-data-table__cell--non-numeric"><?php $answer->getTitle() ?></td>
                <?php
                $count;
                foreach ($userAnswers as $userAnswer) {
                    if ($userAnswer->getQuestionId() == $question->getId()) {
                        $count++;
                    }
                }
                ?>
                <td class="mdl-data-table__cell--non-numeric"><?php $count ?></td>

            </tr>
        <?php endif;
    endforeach; ?>
    </tbody>
</table>

